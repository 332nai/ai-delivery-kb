---
num: "27.4"
topic: 27
slug: refactoring-and-optimization
title_en: "Refactoring and Optimization"
title_zh: "重构与优化"
teaser_en: "30 prompts for refactoring, optimizing, and modernizing existing code."
teaser_zh: "30 条重构 prompt — 重构、性能优化、现代化老代码。"
---

::: en

# Refactoring and Optimization

30 prompts for refactoring, optimizing, and modernizing existing code.

## Refactoring

1. Refactor this code for readability without changing behavior: [paste].
2. Extract reusable functions from this duplication: [paste].
3. Convert this [callback/promise/sync] style to [async/await]: [paste].
4. Replace this if-else chain with a strategy/lookup pattern: [paste].
5. Refactor this class into smaller focused classes: [paste].
6. Add types to this dynamically typed code: [paste].
7. Split this long function into 3-4 well-named helpers: [paste].
8. Replace mutation with immutable patterns in: [paste].
9. Move side effects out of [function/component]: [paste].
10. Convert this OOP code into a functional style: [paste].

## Performance optimization

11. Optimize this loop's time complexity from O(n²) to O(n log n): [paste].
12. Add memoization/caching to this expensive function: [paste].
13. Batch these N+1 queries into a single query: [paste].
14. Replace this nested loop with [map/set/dict] lookup: [paste].
15. Lazy-load [resource] in this component to improve TTI: [paste].
16. Add database indexes for these queries: [paste schema + queries].
17. Convert this synchronous I/O to async/streamed: [paste].
18. Reduce bundle size by code-splitting [routes/components]: [paste config].
19. Identify and remove unnecessary re-renders in: [paste React].
20. Compress and stream this response in [framework]: [paste].

## Modernization

21. Upgrade this code from [old version] to [new version]: [paste].
22. Convert callbacks to Promises, then to async/await: [paste].
23. Replace deprecated [API] with [modern alternative]: [paste].
24. Modernize this jQuery code to vanilla JS / [framework]: [paste].
25. Migrate this Python 2 code to Python 3: [paste].
26. Convert this CommonJS module to ESM: [paste].
27. Replace var with let/const, fix any scoping issues: [paste].
28. Upgrade this class component to a function component with hooks: [paste].
29. Replace XMLHttpRequest with fetch + async/await: [paste].
30. Migrate from [old framework] to [new framework]: [paste]. Highlight changes.

**Total prompts: 30**

:::

::: zh

# 重构与优化

30 条 prompt: 重构、优化、现代化已有代码。

## 重构

1. 在不改变行为的前提下,把这段代码改写得更可读: [粘贴]。
2. 从这段重复中提取可复用函数: [粘贴]。
3. 把这段 [回调/Promise/同步] 风格改成 [async/await]: [粘贴]。
4. 把这段 if-else 链改成策略/查表模式: [粘贴]。
5. 把这个类拆成几个职责单一的小类: [粘贴]。
6. 为这段动态类型代码补类型: [粘贴]。
7. 把这个长函数拆成 3-4 个命名得当的辅助函数: [粘贴]。
8. 把这段里的可变操作换成不可变模式: [粘贴]。
9. 把 [函数/组件] 里的副作用移出去: [粘贴]。
10. 把这段 OOP 代码改成函数式风格: [粘贴]。

## 性能优化

11. 把这个循环时间复杂度从 O(n²) 优化到 O(n log n): [粘贴]。
12. 为这个昂贵函数加 memoization/缓存: [粘贴]。
13. 把这些 N+1 查询合并成一条查询: [粘贴]。
14. 把这段嵌套循环换成 [map/set/dict] 查表: [粘贴]。
15. 在这个组件里懒加载 [资源] 提升 TTI: [粘贴]。
16. 为这些查询加数据库索引: [粘贴 schema 和查询]。
17. 把这段同步 I/O 改成异步/流式: [粘贴]。
18. 通过代码分割 [路由/组件] 缩小 bundle: [粘贴配置]。
19. 在 [粘贴 React] 里识别并消除不必要的重渲染。
20. 在 [框架] 中压缩并流式响应: [粘贴]。

## 现代化

21. 把这段代码从 [旧版本] 升级到 [新版本]: [粘贴]。
22. 把回调改成 Promise,再改成 async/await: [粘贴]。
23. 把弃用的 [API] 换成 [现代替代]: [粘贴]。
24. 把这段 jQuery 现代化为 vanilla JS / [框架]: [粘贴]。
25. 把这段 Python 2 代码迁移到 Python 3: [粘贴]。
26. 把这段 CommonJS 模块转成 ESM: [粘贴]。
27. 把 var 换成 let/const,修复任何作用域问题: [粘贴]。
28. 把这个类组件升级成带 hooks 的函数组件: [粘贴]。
29. 把 XMLHttpRequest 替换为 fetch + async/await: [粘贴]。
30. 从 [旧框架] 迁移到 [新框架]: [粘贴]。标出改动。

**共 30 条 prompt**

:::
