---
num: "28.2"
topic: 28
slug: sql-queries
title_en: "SQL Queries"
title_zh: "SQL 查询"
teaser_en: "28 prompts for SELECT, JOIN, window functions, and optimization."
teaser_zh: "28 条 SQL prompt — 基础查询、连接聚合、窗口函数、优化。"
---

::: en

# SQL Queries

28 prompts for SELECT, JOIN, window functions, and optimization.

## Querying basics

1. Write SQL to return [columns] from [table] where [condition].
2. Generate a SQL query to count [rows] grouped by [dimension].
3. Write SQL to find the top N [items] by [metric] per [category].
4. Generate SQL to find records with no match in [related table].
5. Write SQL filtering for the last [N] days/weeks/months.
6. Build a SQL query that pivots [rows] into [columns].
7. Write SQL returning duplicates by [columns] with counts.
8. Generate SQL that finds records updated since [timestamp].

## Joins and aggregation

9. Write a JOIN between [table A] and [table B] on [key], handle nulls.
10. Build a query joining 3 tables to answer [business question].
11. Generate SQL to compute [metric] with appropriate GROUP BY and HAVING.
12. Write SQL aggregating [metric] by week with proper date truncation.
13. Build a CTE chain that breaks [complex query] into readable steps.
14. Generate a self-join to find [hierarchical relationship] in [table].
15. Write SQL computing percentage of [metric] by [dimension].
16. Build a query showing cohort retention for [user actions].

## Window functions and analytics

17. Write a window function to rank [items] by [metric] within [group].
18. Generate SQL using LAG/LEAD to compute [period-over-period diff].
19. Build a rolling 7-day average for [metric] in [time series table].
20. Write SQL finding first/last event per [user] using ROW_NUMBER.
21. Generate a query computing cumulative [metric] over time.
22. Write SQL to identify users whose [metric] changed by more than [X%].
23. Build a percentile/median query for [metric] by [group].
24. Generate SQL bucketing values in [column] into [N] equal-width bins.

## Optimization

25. Suggest indexes for this slow query: [paste query and schema].
26. Rewrite this query to avoid full table scan: [paste].
27. Explain the EXPLAIN plan of this query and what to fix: [paste].
28. Identify N+1 query patterns in [application code]: [paste].

**Total prompts: 28**

:::

::: zh

# SQL 查询

28 条 prompt: SELECT、JOIN、窗口函数、优化。

## 查询基础

1. 写 SQL,从 [表] 中按 [条件] 返回 [列]。
2. 生成 SQL,按 [维度] 统计 [行数]。
3. 写 SQL,在每个 [分类] 里按 [指标] 取前 N 个 [项目]。
4. 生成 SQL,找出在 [关联表] 里没有匹配的记录。
5. 写 SQL,筛选最近 [N] 天/周/月。
6. 写一个把 [行] 透视成 [列] 的 SQL 查询。
7. 写 SQL,按 [列] 找重复并带条数。
8. 生成 SQL,找出 [时间戳] 之后更新的记录。

## 连接与聚合

9. 在 [A 表] 和 [B 表] 上按 [键] 写 JOIN,处理 null。
10. 写一个 3 表连接的查询,回答 [业务问题]。
11. 生成 SQL,带合适的 GROUP BY 和 HAVING 计算 [指标]。
12. 写 SQL 按周聚合 [指标],日期截断要对。
13. 用 CTE 把 [复杂查询] 拆成可读的几步。
14. 用自连接找出 [表] 中的 [层级关系]。
15. 写 SQL,按 [维度] 计算 [指标] 的占比。
16. 写一个查询展示 [用户行为] 的 cohort 留存。

## 窗口函数与分析

17. 用窗口函数在 [分组] 内按 [指标] 给 [项目] 排名。
18. 用 LAG/LEAD 计算 [环比差]。
19. 为 [时间序列表] 的 [指标] 写 7 日滚动平均。
20. 用 ROW_NUMBER 找出每个 [用户] 的首次/末次事件。
21. 生成 SQL,计算 [指标] 随时间的累计值。
22. 写 SQL,找出 [指标] 变化超过 [X%] 的用户。
23. 为 [分组] 写 [指标] 的中位数/百分位查询。
24. 生成 SQL,把 [列] 值分成 [N] 个等宽桶。

## 优化

25. 为这条慢查询建议索引: [粘贴查询和 schema]。
26. 改写这条查询,避免全表扫描: [粘贴]。
27. 解释这条查询的 EXPLAIN 计划,该修哪里: [粘贴]。
28. 在 [应用代码] 里识别 N+1 查询模式: [粘贴]。

**共 28 条 prompt**

:::
