---
num: "28.3"
topic: 28
slug: python-data-analysis
title_en: "Python Data Analysis"
title_zh: "Python 数据分析"
---

::: en

# Python Data Analysis

28 prompts for pandas, numpy, scripting around data tasks.

## Loading and shaping

1. Write pandas code to load [file path/format] with proper dtypes.
2. Reshape this DataFrame from wide to long for [columns]: [describe].
3. Merge two DataFrames on [keys] handling many-to-many correctly.
4. Convert this [JSON/XML/CSV] response into a clean DataFrame: [paste].
5. Stack/concat these N DataFrames with mismatched columns.
6. Pivot this DataFrame to show [metric] by [row dim] and [col dim].
7. Read [N] CSVs from [folder] and combine into one DataFrame.
8. Sample [N] rows stratified by [column] from a DataFrame.

## Cleaning and transforming

9. Fill missing values in [column] using [strategy].
10. Standardize text in [column]: trim, lowercase, remove punctuation.
11. Detect and remove outliers in [column] using [IQR/zscore].
12. Convert [column] of mixed types to consistent [target type].
13. Apply [function] to each row efficiently (avoid iterrows).
14. One-hot encode [categorical column] in pandas.
15. Bin [continuous column] into [N] equal-frequency buckets.
16. Parse dates in [column] handling multiple formats.

## Analysis and stats

17. Compute summary stats by group for [metric] in [DataFrame].
18. Run a correlation analysis between [column A] and [column B].
19. Build a cohort retention analysis from [event data].
20. Compute month-over-month growth rate for [metric].
21. Run a t-test comparing [group A] vs [group B] on [metric].
22. Identify the top 10 drivers of [outcome] using a feature importance check.
23. Detect seasonality in [time series] using a simple decomposition.
24. Build a funnel from event data with these stages: [list].

## Scripts and automation

25. Write a Python script that watches [folder] and processes new files.
26. Generate a script that pulls [API], transforms, and writes to [destination].
27. Build a Python CLI to run [analysis] with configurable params.
28. Schedule a Python job using [cron/airflow] running [task] daily.

**Total prompts: 28**

:::

::: zh

# Python 数据分析

28 条 prompt: pandas、numpy、数据相关脚本。

## 加载与整形

1. 写 pandas 代码,按合适的 dtype 加载 [文件路径/格式]。
2. 把这个 DataFrame 从宽改长,针对 [列]: [描述]。
3. 在 [键] 上合并两个 DataFrame,正确处理多对多。
4. 把这段 [JSON/XML/CSV] 响应转成干净的 DataFrame: [粘贴]。
5. 把这 N 个列名不一致的 DataFrame 堆叠/拼接。
6. 把这个 DataFrame 透视成 [行维度] × [列维度] 显示 [指标]。
7. 从 [文件夹] 读 [N] 个 CSV 并合成一个 DataFrame。
8. 按 [列] 分层抽 [N] 行。

## 清洗与转换

9. 用 [策略] 填充 [列] 的缺失值。
10. 标准化 [列] 文本: 去空格、小写、去标点。
11. 用 [IQR/zscore] 检测并去除 [列] 异常值。
12. 把混杂类型的 [列] 统一成 [目标类型]。
13. 高效对每行应用 [函数] (避免 iterrows)。
14. 在 pandas 里对 [类别列] 做 one-hot 编码。
15. 把 [连续列] 按等频分成 [N] 个桶。
16. 解析 [列] 里多种格式的日期。

## 分析与统计

17. 为 [DataFrame] 的 [指标] 按分组计算汇总统计。
18. 对 [A 列] 和 [B 列] 做相关性分析。
19. 用 [事件数据] 构建 cohort 留存分析。
20. 为 [指标] 计算环比增长率。
21. 对 [A 组] 和 [B 组] 的 [指标] 做 t 检验。
22. 用特征重要性方法识别 [结果] 的前 10 个驱动因素。
23. 用简单分解检测 [时间序列] 的季节性。
24. 用事件数据构建漏斗,阶段: [列表]。

## 脚本与自动化

25. 写一个 Python 脚本监听 [文件夹],处理新文件。
26. 生成一个脚本,拉 [API],转换后写到 [目标]。
27. 用 Python 写一个可配置参数的 CLI 跑 [分析]。
28. 用 [cron/Airflow] 调度一个 Python 任务,每天跑 [任务]。

**共 28 条 prompt**

:::
